package co.skliba.rideme.ui.dashboard.settings

import co.skliba.rideme.ui.base.BasePresenter
import javax.inject.Inject

class SettingsPresenter @Inject constructor(view: SettingsView) : BasePresenter<SettingsView>(view) {

    fun init() {
    }

    override fun cancel() {
    }
}